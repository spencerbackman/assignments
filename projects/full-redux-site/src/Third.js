import React from 'react';
import { connect } from 'react-redux';

class Third extends React.Component {
    render() {
        return (
            <div>

            </div>
        )
    }
}

export default connect(state => state, {  })(Third)